// make a function add that adds two numbers togehter
// add(1,2) // => 3

// list three different ways to write a function like add
// 1
// 2
// 3

// make a function that takes a number and returns true if even otherwise false
// isEven(2) // true

// make a function that takes a string and returns true if it has a vowel
// isVowel('a') // true

// make a function that takes a boolean or truthy/falsey value and returns the opposite of it
// flip(true) // false
// flip('') // true

// make a function that takes a string and returns true if it can find b

// hasB('ab') // true

// make a function that takes a string and character 
// and returns true if it finds it otherwise false
// hasChar('abc', 'a') // true


// make a function range that takes two numbers and returns an array
// starting from the first and going up to the last
// range(1,3) // => [1,2,3]

// make a function that takes an id and array and finds an object with that id
// findId(1, [{id: 1}, {id: 2}]) // {id: 1}

// make a function that takes an array of letters and
// returns an object with the count of all letters
// letterCount(['a', 'a', 'b']) // {a: 2, b: 1}

// make a function that collects the letter a
// collectA('abcdea') // ['a', 'a']

// make a function that gets count of the letter a
// countA('abcdea') // 2

// make a function that gets count of the letter a
// countA2(['a', 'a', 'a', 'b']) // 3

// make a function called vowelMap that gets the count of vowel
// and nonvowel characters
// vowelMap('abc hello') // {vowel: 3, nonvowel: 5}

// make a function that takes an array and number and calls another
// function which gets that number when found
/*
  findIt([1,2,3], 1, function (num) {
    console.log('found the number ' + num) // found the number 1
  })
*/


// make a function which takes a function and its arguments and return the value
// callIt(add, 1, 2) // 3

// make a recursive function isEven2 where N is number
// 0 is true
// 1 is false
// every other number can be thought of N - 2 For example...
// 4 - 2 - 2 is 0 so it is even
// 5 - 2 - 2 is 1 so it is odd


// make a recursive function (function that calls itself) that returns a factorial of a number
// factorial(3) // is 6 because 3 * 2 * 1
// factorial(4) // 24 because (4 * 3 * 2 * 1)
